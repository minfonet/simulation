{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "lead",
  "agent": {
    "lead": {
      "description": "Coordinates software delivery, breaks work into tasks, delegates to implementer and reviewer.",
      "mode": "subagent"
    },
    "implementer": {
      "description": "Writes clean working code following MVP specifications.",
      "mode": "subagent"
    },
    "reviewer": {
      "description": "Critically evaluates implementations for correctness, maintainability, and security.",
      "mode": "subagent"
    },
    "qa": {
      "description": "Creates and runs tests for all layers (frontend, backend, E2E).",
      "mode": "subagent"
    }
  }
}
